package com.eburg_soft.televisionbroadcasting.domain.usecases

import com.eburg_soft.televisionbroadcasting.data.repository.TVRepository
import javax.inject.Inject

class UpdateDaysUseCase @Inject constructor(private val repository: TVRepository) {

//    fun execute(daysList: List<DayEntity>): Completable = repository.updateDays(daysList)
}