package com.eburg_soft.televisionbroadcasting.domain.usecases

import com.eburg_soft.televisionbroadcasting.data.repository.TVRepository
import javax.inject.Inject

class UpdateGroupsUseCase @Inject constructor(private val repository: TVRepository) {

//    fun execute(groupList: List<GroupEntity>): Completable = repository.updateGroups(groupList)
}